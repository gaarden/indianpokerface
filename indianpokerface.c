#include <stdio.h>
//소켓 서버 만들기
//소켓연결
//게임시작
void startGame() {
	//환영 문구, 규칙 설명
	//시작 키를 입력하면 게임 화면 진입
	//숫자 배분 → rand 함수로 나눠주고 화면에 각자 띄우기(화면에 띄워진 게 상대방 숫자)
	//누구부터할지 어떻게 정하지
	//기본칩 베팅(아마 1개씩)
	//누구차롄지 명시하고 레이드, 홀드, 포기 정하기(칩개수 정하기)
	//홀드 나올때까지 턴넘기기 반복
	//턴이 넘어가면 내 화면은 아무것도 못누르게 시그널 막기(상대도 마찬가지)
	//홀드 나오면 상대방도 같은 수를 베팅or 포기하게 하기
	//카드까기
	//누가 이겼는지, 칩 주는 모션
	//반복
	//끝!
	//한번더할건가요 ? (y / n)
}
//소켓종료
int main()
{
	
}
